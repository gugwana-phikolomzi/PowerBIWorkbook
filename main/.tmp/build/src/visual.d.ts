import "./components/style.less";
import powerbi from "powerbi-visuals-api";
import IVisual = powerbi.extensibility.visual.IVisual;
import VisualConstructorOptions = powerbi.extensibility.visual.VisualConstructorOptions;
import VisualUpdateOptions = powerbi.extensibility.visual.VisualUpdateOptions;
export declare class Visual implements IVisual {
    private readonly hostElement;
    private readonly root;
    private dataView?;
    private availableFields;
    private draggedField?;
    private draggedWellField?;
    private fieldWellsState;
    constructor(options: VisualConstructorOptions);
    update(options: VisualUpdateOptions): void;
    private render;
    private renderWorkspace;
    private handleFieldDragStart;
    private handleFieldDragEnd;
    private handleWellFieldDragStart;
    private handleWellFieldDragEnd;
    private handleFieldMove;
    private handleFieldRemove;
    private removeFieldFromAllWells;
    private removeUnavailableFieldsFromWells;
    private getAvailableUnassignedFields;
    private clearDropZoneHighlights;
    private renderPivotArea;
    private renderTableWireframe;
    private createElement;
}
